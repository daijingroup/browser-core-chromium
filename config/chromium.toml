schema_version = 1
engine = "chromium"
source = "https://chromium.googlesource.com/chromium/src.git"
state = "uninitialized"

# Set both fields when the first supported Chromium revision is selected.
revision = ""
milestone = 0
